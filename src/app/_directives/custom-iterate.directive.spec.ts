import { CustomIterateDirective } from './custom-iterate.directive';

describe('CustomIterateDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomIterateDirective();
    expect(directive).toBeTruthy();
  });
});
